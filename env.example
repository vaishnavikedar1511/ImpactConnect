# Contentstack Configuration
# Copy this file to .env.local and fill in your values

# =============================================================================
# CONTENTSTACK CMS
# =============================================================================

# Contentstack API Key (found in Settings > Stack)
CONTENTSTACK_API_KEY=your_api_key_here

# Contentstack Delivery Token (found in Settings > Tokens)
CONTENTSTACK_DELIVERY_TOKEN=your_delivery_token_here

# Contentstack Environment (e.g., production, development)
CONTENTSTACK_ENVIRONMENT=development

# Contentstack Region (na, eu, azure-na, azure-eu)
# Leave empty for North America (default)
CONTENTSTACK_REGION=

# Optional: Branch for branching feature
CONTENTSTACK_BRANCH=main

# =============================================================================
# CONTENTSTACK MANAGEMENT API (Event Submission)
# =============================================================================
# Required for user-submitted events feature
# Management Token allows creating draft entries in Contentstack

# Management Token (found in Settings > Tokens > Management Tokens)
# Required permissions: Read/Write on your stack
CONTENTSTACK_MANAGEMENT_TOKEN=

# Asset folder UID for uploaded event images (optional)
# Find it: Assets > Click folder > Check URL for the UID
CONTENTSTACK_ASSETS_FOLDER_UID=

# =============================================================================
# CONTENTSTACK AUTOMATE (Email Notifications)
# =============================================================================
# Configure these to enable email notifications via Contentstack Automate
# Leave empty to use mock/logging mode (for development)

# Webhook URL for participant confirmation emails
# Create a webhook automation in Contentstack Automate that sends emails
CONTENTSTACK_AUTOMATE_PARTICIPANT_WEBHOOK=

# Webhook URL for organizer notification emails
CONTENTSTACK_AUTOMATE_ORGANIZER_WEBHOOK=

# =============================================================================
# CONTENTSTACK PERSONALIZE
# =============================================================================
# Optional: Enable personalized content based on user attributes
# Get your Personalize Project UID from: Personalize > Settings > General > Project Details

# Personalize Project UID (24-character UID from Personalize Settings)
# This is different from API keys - it's the project identifier
NEXT_PUBLIC_CONTENTSTACK_PERSONALIZE_PROJECT_UID=

# Optional: Custom Edge API URL (only if using non-standard region)
# Defaults based on CONTENTSTACK_REGION:
# - AWS NA: https://personalize-edge.contentstack.com
# - AWS EU: https://eu-personalize-edge.contentstack.com
# - Azure NA: https://azure-na-personalize-edge.contentstack.com
# - Azure EU: https://azure-eu-personalize-edge.contentstack.com
# NEXT_PUBLIC_CONTENTSTACK_PERSONALIZE_EDGE_API_URL=

# Webhook URL for event submission (admin approval flow)
# Get this URL from Contentstack Automate when you create the HTTP trigger
CONTENTSTACK_AUTOMATE_EVENT_WEBHOOK=

# Webhook URL for approval/rejection decision
# Single automation with conditional paths for approve and reject
CONTENTSTACK_AUTOMATE_DECISION_WEBHOOK=

# Webhook URL for registration confirmation emails
CONTENTSTACK_AUTOMATE_REGISTRATION_WEBHOOK=

# Webhook URL for event APPROVAL (creates entry + sends approval email)
CONTENTSTACK_AUTOMATE_APPROVE_WEBHOOK=

# Webhook URL for event REJECTION (sends rejection email)
CONTENTSTACK_AUTOMATE_REJECT_WEBHOOK=

# Your app's public URL (for approval links in emails)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: API key for webhook authentication
CONTENTSTACK_AUTOMATE_API_KEY=

# =============================================================================
# WEBHOOK REVALIDATION
# =============================================================================
# Secure your revalidation endpoints with secrets

# Secret for Contentstack webhook validation
# Set this in Contentstack webhook headers as x-webhook-secret
CONTENTSTACK_WEBHOOK_SECRET=

# Secret for manual revalidation API (/api/revalidate?secret=xxx)
REVALIDATION_SECRET=

# =============================================================================
# ALGOLIA SEARCH
# =============================================================================
# Configure Algolia for smart search functionality
# Sign up at https://www.algolia.com/ (free tier available)

# Algolia Application ID (found in API Keys section)
NEXT_PUBLIC_ALGOLIA_APP_ID=

# Algolia Search-Only API Key (safe for frontend use)
# Do NOT use your Admin API Key here
NEXT_PUBLIC_ALGOLIA_SEARCH_API_KEY=

# Algolia Admin API Key (for indexing - server-side only)
# Keep this secret! Only use on server/build scripts
ALGOLIA_ADMIN_API_KEY=
